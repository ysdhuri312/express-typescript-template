# express-typscript-template

To install dependencies:

```bash
npm install
```

To run:

```bash
npm run index.ts
```

This project was created using `npm init` . [NPM](https://npm.com) is a fast all-in-one JavaScript runtime.
